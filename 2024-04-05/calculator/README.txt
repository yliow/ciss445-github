to compile:

    make

to run in bash shell:

    make r

to run with inputs from stdin.txt:

    make r < stdin.txt

to run in utop: in utop execute

    #use "utop-main.ml";;

to remove generate files:

   make c
   
